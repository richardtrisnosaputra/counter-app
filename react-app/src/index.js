import React from "react";
import ReactDOM from "react-dom";

const element = <h1>Hello World from clone</h1>;
ReactDOM.render(element, document.getElementById("root"));

const element_p1 = <p>This is my first React.JS app</p>;
ReactDOM.render(element_p1, document.getElementById("p1"));
